[project]
name = "python-app"
description = "Python fixture web service"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27"
]

[project.scripts]
python-app = "myapp.cli:main"

[tool.uvicorn]
port = 8000